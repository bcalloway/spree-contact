- content_for(:head) do
  = stylesheet_link_tag 'contact.css'

%h1
  Contact Us
%p
  Interested in contacting us?
  %br
  Have questions?
  %br
  Please use the form below.


- semantic_form_for @contact do |f|
  /= f.error_messages

  - f.inputs :class => "contact-form" do
    = f.input :name, :required => true
    = f.input :email, :required => true
    = f.input :city, :required => false
    = f.input :state, :label => "State", :required => false, :collection => State.find(:all, :order => "name ASC"), :as => :select, :prompt => "--Select a State--"
    = f.input :zip, :required => false, :input_html => { :size => 12 }
    = f.input :message, :required => true
    = f.input :optin, :label => "Yes, please put me on your mailing list", :as => :boolean, :required => false, :input_html => { :checked => "checked" }
  - f.buttons do
    = f.commit_button "Submit"
